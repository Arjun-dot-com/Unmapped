"use client";
import {useEffect,useRef,useState} from "react";
export default function CesiumViewer(){
 const ref=useRef<HTMLDivElement>(null), points=useRef<any[]>([]); const [distance,setDistance]=useState<number|null>(null); const [confidence,setConfidence]=useState(false);
 useEffect(()=>{let viewer:any,handler:any,stopped=false;(async()=>{try{const C=await import("@cesium/engine");if(stopped||!ref.current)return;viewer=new C.CesiumWidget(ref.current,{scene3DOnly:true,shouldAnimate:false,baseLayer:false,globe:new C.Globe(C.Ellipsoid.WGS84),showRenderLoopErrors:false,contextOptions:{webgl:{failIfMajorPerformanceCaveat:false}}});handler=new C.ScreenSpaceEventHandler(viewer.scene.canvas);handler.setInputAction((m:any)=>{const p=viewer.scene.pickPosition(m.position);if(!p)return;points.current.push(p);if(points.current.length===2){const[a,b]=points.current;setDistance(C.Cartesian3.distance(a,b));points.current=[]}},C.ScreenSpaceEventType.LEFT_CLICK);const meta=await (await fetch("/api/models/demo/geospatial_meta.json")).json(),o=meta.origin;const model=await C.Model.fromGltfAsync({url:"/api/models/demo/model.glb",modelMatrix:C.Transforms.eastNorthUpToFixedFrame(C.Cartesian3.fromDegrees(o.lon,o.lat,o.alt))});viewer.scene.primitives.add(model);viewer.camera.flyTo({destination:C.Cartesian3.fromDegrees(o.lon,o.lat,o.alt+180),orientation:{heading:0,pitch:-C.Math.PI_OVER_TWO,roll:0}})}catch(e){console.warn("Cesium unavailable; viewer shell remains usable",e)}})();return()=>{stopped=true;handler?.destroy?.();viewer?.destroy?.()}},[]);
 return <div className="viewer"><div ref={ref} className="globe"/><section className="hud"><div className="badge">LIVE 3D RECONSTRUCTION</div><div>Click twice to measure</div>{distance!==null&&<div>Distance: {distance.toFixed(2)} m</div>}<button onClick={()=>setConfidence(!confidence)}>{confidence?"Hide":"Show"} low-confidence</button>{confidence&&<div style={{color:"#ff6868"}}>● Occlusion overlay enabled</div>}</section></div>
}
