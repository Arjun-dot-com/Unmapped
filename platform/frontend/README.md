# Phase 5 viewer

Install with `npm install`, start with `npm run dev`, and point the frontend at the FastAPI server. The viewer loads a Cesium 3D Tiles endpoint and includes click-to-measure and confidence-layer controls.
